# Repartidor Front

Frontend para modulo de repartidores conectado al backend POS.

## Ejecutar

1. `npm install`
2. `npm run dev`

## Variables

- `VITE_BACKEND_URL` (ejemplo: `http://localhost:5000/api`)

Si no se define, usa `http://localhost:5000/api`.